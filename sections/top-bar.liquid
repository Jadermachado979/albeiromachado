{% schema %}
{
  "name": "Topbar Gymshark",
  "tag": "section",
  "class": "topbar-gymshark",
  "settings": [
    {
      "type": "url",
      "id": "link_account",
      "label": "Enlace de Account"
    },
    {
      "type": "url",
      "id": "link_accessibility",
      "label": "Enlace de Accessibility Statement"
    },
    {
      "type": "url",
      "id": "link_help",
      "label": "Enlace de Help"
    },
    {
      "type": "url",
      "id": "link_email",
      "label": "Enlace de Email Sign Up"
    },
    {
      "type": "url",
      "id": "link_blog",
      "label": "Enlace de Blog"
    }
  ],
  "presets": [
    {
      "name": "Topbar Gymshark",
      "category": "Header"
    }
  ]
}
{% endschema %}

<style>
.topbar-gymshark {
  background-color: #f7f7f7;
  border-bottom: 1px solid #e5e5e5;
  font-size: 14px;
  padding: 8px 20px;
}

.topbar-gymshark .container {
  display: flex;
  justify-content: flex-end;
  align-items: center;
  flex-wrap: wrap;
  gap: 16px;
}

.topbar-gymshark a {
  color: #444;
  text-decoration: none;
  transition: color 0.2s ease;
}

.topbar-gymshark a:hover {
  color: #111;
}

.topbar-gymshark select {
  background: transparent;
  border: none;
  font-size: 14px;
  color: #444;
  cursor: pointer;
}
</style>

<div class="topbar-gymshark">
  <div class="container">
    {% if section.settings.link_account != blank %}
      <a href="{{ section.settings.link_account }}">Account</a>
    {% endif %}
    {% if section.settings.link_accessibility != blank %}
      <a href="{{ section.settings.link_accessibility }}">Accessibility Statement</a>
    {% endif %}
    {% if section.settings.link_help != blank %}
      <a href="{{ section.settings.link_help }}">Help</a>
    {% endif %}
    {% if section.settings.link_email != blank %}
      <a href="{{ section.settings.link_email }}">Email Sign Up</a>
    {% endif %}
    {% if section.settings.link_blog != blank %}
      <a href="{{ section.settings.link_blog }}">Blog</a>
    {% endif %}

    <div style="display: flex; align-items: center; gap: 8px;">
      <span>🇺🇸</span>
      <select>
        <option>US</option>
        <option>UK</option>
        <option>EU</option>
      </select>
    </div>

    <div style="display: flex; align-items: center; gap: 8px;">
      <select>
        <option>English</option>
        <option>Español</option>
        <option>Français</option>
      </select>
    </div>
  </div>
</div>

